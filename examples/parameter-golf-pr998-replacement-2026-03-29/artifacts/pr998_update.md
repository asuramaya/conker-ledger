PR `#998` should stay the same pull.

This update replaces the invalid old `Conker-5 tandem` story with a smaller, honest packet:

- fresh legal `Conker-11` rerun: `2.0124189795892984 bpb`
- provenance disclosure: selected best of `2` fresh legal runs
- targeted `conker-detect` structural audit: extracted `causal_mask` has `upper_frac = 0.0`, `diag_frac = 0.0`, and exact Toeplitz strict-lower geometry
- public validity bundle and living repos:
  - `conker`: <https://github.com/asuramaya/conker>
  - `conker-detect`: <https://github.com/asuramaya/conker-detect>
  - `conker-ledger`: <https://github.com/asuramaya/conker-ledger>
  - `giddy-up`: <https://github.com/asuramaya/giddy-up>

Suggested update text for the PR conversation:

> Updating this same PR with a corrected validity packet. The old `Conker-5 tandem` score is no longer the claim I stand behind. The attached replacement evidence is a fresh legal `Conker-11` rerun at `2.0124 bpb`, selected as the best of `2` fresh legal runs and accompanied by a `conker-detect` audit of the saved checkpoint's extracted causal mask (`upper_frac = 0`, `diag_frac = 0`, perfect strict-lower Toeplitz geometry). This is a modest score, but it is the one I currently trust. Canonical source repos: `conker`, `conker-detect`, `conker-ledger`, and `giddy-up`.
